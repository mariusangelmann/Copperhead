[default]
exten => 600,1,Answer()
 same => n,Echo()
 same => n,Hangup()

exten => _+!,1,NoOp(Incoming call to ${EXTEN})
 same => n,Answer()
 same => n,Wait(300)
 same => n,Hangup()

exten => _X.,1,NoOp(Incoming call to ${EXTEN})
 same => n,Answer()
 same => n,Wait(300)
 same => n,Hangup()
